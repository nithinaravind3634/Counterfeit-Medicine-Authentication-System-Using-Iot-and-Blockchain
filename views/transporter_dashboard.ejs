<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transporter Dashboard</title>
    <style>
        body {
            background-image: url('media/transporter_dashboard.jpg'); /* Relative path to the background image */
            background-size: cover;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: flex-start; /* Adjusted to start content from the left side */
            align-items: center;
            height: 100vh;
            color: #fff; /* Text color */
        }

        #transporterDashboard {
            background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent background for better readability */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            text-align: center; /* Adjusted to align text to the left */
            margin-left: 20%;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #fff;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        input[type="submit"] {
            background-color: #4caf50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }

        h1, h2 {
            color: #fff;
        }
    </style>
</head>
<body>
    <div id="transporterDashboard">
        <h1>Transporter Dashboard</h1>
        <!-- Add content specific to the transporter dashboard -->
        <h2>Enter Package Details</h2>
        <form method="post" action="/order">
            <label for="packageAddress">Destination:</label>
            <input type="text" name="packageAddress" placeholder="Enter Supplier Address" required><br>

            <label for="transporterType">Source:</label>
            <input type="text" name="transporterType" placeholder="Enter Manufacturer Address" required><br>

            <label for="transporterAddress">Transporter Name:</label>
            <input type="text" name="transporterAddress" required><br>

            <input type="submit" value="Submit">
        </form>
    </div>
</body>
</html>
